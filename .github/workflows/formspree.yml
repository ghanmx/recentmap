name: Formspree Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy
        uses: formspree/cli-action@v1
        with:
          deployKey: ${{ secrets.FORMSPREE_DEPLOY_KEY }}
